### Jueves 30 de mayo

- Ejercicio 1:
  
  En el ejercicio uno, vemos el simbolo +=

```python
palabra += caracter ( esto quiere decir palabra = palabra + caracter)
```

¿Qué sucede en este ejercicio?

Lo que podemos ver a través de es este ejercicio se como se recorre el código a través de un for I, se utiliza con un for() que permite recorrer el texto. 

A través de esto podemos utilizar in para detectar cuando tiene espacios o no y por ultimo usamos un strip() para sacar los espacios.

De esa podemos ver donde se ubican lo espacios para obtener cuales palabras hay que invertir.

- Ejercicio dos
  
  - Se hace similar al ejercicio numero 1 y se copia la parte del per se incluye que la primera letra se ponga en mayúsculas y las demás son todas falsas, esto se hace a través de probar con bandera que permita hacer esto.. 

- Ejercicio tres
  
  - Permite 

- Ejercicio cuatro
  
  - Me falto poner el cero

### Listas

¿Qué es una lista?

Es un objeto formado, que contiene distintos tipos de elementos, pueden existir listas anidadas.

¿Qué son los objetos?

Se basan en clases, permiten almacenar información de gran manera.

¿Qué hacen

¿Cómo se acceda a los elementos de una lista, se hace a través de un indíce, que permite obtener sus valores interiores, funciona igual que 

¿Cómo se puede recorrer una lista?

Se puede recorrer una lista a través de for *utilizando*

**for in**

### ¿Qué tipos de indices se pueden usar?

Se pueden utilizar negativos, o positvos, recordar que cuando se utilizan las slices siempre es uno menos.

### ¿Cómo se invierte una lista con indices?

[::-1] --> Permite que [a,b,c] sea [c,b,a]

#### ¿Por qué es útil que una lista sea mutable?

Permite usarse para cambiar las posiciónes

## Métodos:

##### ¿Qué métodos tienen la lista?

Se puede utilizar *append() que permite agregar  lo que este dentro del parenteisis como un elemento en una lista;

*In* a pesar de ser un operador booleano que devuelve **True** or **False** si lo que esta dentro se encuentra en la lista

- extend permite concatener dos listas.

- del se puede utilizar para eliminar elementos que esta como puesto como el elemento, esto se hace con parentesis recto entre medio usando posiciónes individuales o con slices.

- sort.() permite  ordenar la lista si los elementos son del mismo tipo

- count() permite contar los elementos en la lista

- max y min permiten hallar el máximo y menor.
